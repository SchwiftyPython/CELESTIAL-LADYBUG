using UnityEngine;

namespace Assets.Scripts.UI
{
    public class OnMouseOverAbilityButton : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

    
    }
}
